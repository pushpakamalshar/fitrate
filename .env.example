NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Required for AI outfit analysis (server-side only, never exposed to the client)
OPENAI_API_KEY=
# Optional override, defaults to gpt-4o-mini
OPENAI_MODEL=
